OPENAI_API_KEY=<>

# Cognito (Railway / production)
COGNITO_REGION=<>
COGNITO_USER_POOL_ID=<>
COGNITO_APP_CLIENT_ID=<>
COGNITO_USER_ATTRIBUTE=<>
COGNITO_APPROVED_VALUE=<>

# Comma-separated frontend origins (Amplify + local dev)
ALLOWED_ORIGINS=<>

# Local dev only: skip JWT when Cognito vars are unset or AUTH_DISABLED=true
AUTH_DISABLED=false
